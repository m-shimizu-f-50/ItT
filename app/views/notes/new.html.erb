<h1>新規登録</h1>
<%= form_for(@note) do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>カテゴリー</h4>
  <%= f.collection_select :category_id, Category.all, :id, :name %>

  <h4>内容</h4>
  <%= f.text_area :explanation %>

  <%= f.submit '登録' %>
<% end %>