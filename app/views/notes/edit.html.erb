<h1>編集画面</h1>
<%= form_for(@note) do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>カテゴリ</h4>
  <%= f.collection_select :category_id, Category.all, :id, :name %>
  <h4>本文</h4>
  <%= f.text_area :explanation %>
  <%= f.submit '保存' %>
<% end %>