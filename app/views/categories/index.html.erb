<div class='container'>
  <div class='row'>
    <h2>カテゴリー一覧</h2>
    <div class='col-xs-12'>
      <div class='row'>
        <%= form_for(@category, url:'/categories') do |f| %>
          <div class='category-field'>
            <%= f.label :name ,'カテゴリー名' %>
            <%= f.text_field :name %>
          </div>
          <div class='category-button'>
            <%= f.radio_button :is_valid, :有効 %>
            <%= f.label :有効 %>
            <%= f.radio_button :is_valid, :無効 %>
            <%= f.label :無効 %>
            <%= f.submit '追加', class:'btn btn-info' %>
          </div>
        <% end %>
      </div>

      <table class='table'>
        <thead>
          <th>カテゴリ</th>
          <th>状態</th>
          <th></th>
        </thead>
        <tbody>
          <% @categorys.each do |category| %>
          <tr>
            <td><%= category.name %></td>
            <td><%= category.is_valid %></td>
            <td><%= link_to '編集する',edit_category_path(category.id) %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>